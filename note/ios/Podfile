platform :ios, '13.0'

# Tắt thống kê của CocoaPods (tùy chọn)
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

require_relative '../.ios/Flutter/podhelper.rb'

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end
